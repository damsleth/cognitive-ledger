{
  "name": "cognitive-ledger-ux",
  "version": "0.0.0",
  "private": true,
  "description": "Dev tooling for iterating on the cognitive-ledger web UI (uxl + playwright). The product itself is Python; this package.json only carries the screenshot/review loop.",
  "type": "module",
  "scripts": {
    "dev": "./.venv/bin/python -m ledger web --port 5173 --log-level warning"
  },
  "devDependencies": {
    "@damsleth/ux-loop": "file:../ux-loop",
    "playwright": "^1.48.0"
  }
}
